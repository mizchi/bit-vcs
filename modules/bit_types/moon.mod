name = "mizchi/bit_types"

version = "0.45.7"

import {
  "mizchi/bit_object@0.45.7",
}

repository = "https://github.com/mizchi/bit-vcs"

license = "Apache-2.0"

keywords = [ "git", "types" ]

description = "Shared types & traits (re-exports from bit_object)"

source = "src"

preferred_target = "native"
