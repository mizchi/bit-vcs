name = "mizchi/bit_diff3"

version = "0.46.2"

repository = "https://github.com/mizchi/bit-vcs"

license = "Apache-2.0"

keywords = [ "git", "merge", "diff3" ]

description = "3-way merge primitives (gix-merge low-level equivalent)"

source = "src"

preferred_target = "native"
