name = "mizchi/bit_diff_core"

version = "0.46.3"

repository = "https://github.com/mizchi/bit-vcs"

license = "Apache-2.0"

keywords = [ "git", "diff" ]

description = "Low-level diff algorithm (gix-diff equivalent)"

source = "src"

preferred_target = "native"
