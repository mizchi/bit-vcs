name = "mizchi/bit_config"

version = "0.46.3"

import {
  "mizchi/bit_utils@0.46.3",
}

repository = "https://github.com/mizchi/bit-vcs"

license = "Apache-2.0"

keywords = [ "git", "config", "gitconfig" ]

description = "Git config file parser (gix-config equivalent)"

source = "src"

preferred_target = "native"
