name = "mizchi/bitx_bitconfig"

version = "0.45.7"

import {
  "bobzhang/toml@0.1.7",
  "moonbitlang/x@0.4.40",
}

repository = "https://github.com/mizchi/bit-vcs"

license = "Apache-2.0"

keywords = [ "git", "config" ]

description = "bit-specific config (extension module for mizchi/bit)"

source = "src"

preferred_target = "native"
