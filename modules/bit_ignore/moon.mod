name = "mizchi/bit_ignore"

version = "0.46.3"

repository = "https://github.com/mizchi/bit-vcs"

license = "Apache-2.0"

keywords = [ "git", "gitignore", "glob" ]

description = ".gitignore + glob matching (gix-ignore + gix-glob equivalent)"

source = "src"

preferred_target = "native"
