name = "mizchi/bit_apply"

version = "0.46.3"

repository = "https://github.com/mizchi/bit-vcs"

license = "Apache-2.0"

keywords = [ "git", "patch", "apply" ]

description = "Unified-diff patch application"

source = "src"

preferred_target = "native"
