name = "mizchi/bit_date"

version = "0.45.7"

repository = "https://github.com/mizchi/bit-vcs"

license = "Apache-2.0"

keywords = [ "git", "date", "rfc2822" ]

description = "Git date parsing primitives (gix-date equivalent)"

source = "src"

preferred_target = "native"
