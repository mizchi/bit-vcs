name = "mizchi/bit_bootstrap"

version = "0.45.7"

repository = "https://github.com/mizchi/bit-vcs"

license = "Apache-2.0"

keywords = [ "git", "bootstrap" ]

description = "Native-stub bootstrap helpers (stdio guard)"

source = "src"

preferred_target = "native"
