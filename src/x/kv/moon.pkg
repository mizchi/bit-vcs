import {
  "mizchi/bit" @git,
  "mizchi/bit/lib" @lib,
  "moonbitlang/core/buffer" @buffer,
}

import {
  "moonbitlang/core/bench" @bench,
  "mizchi/bit/x/fs" @fs,
} for "test"

warnings = "-29"

options(
  targets: {
    "bench_test.mbt": [ "native" ],
    "kv_test.mbt": [ "native" ],
    "sync_test.mbt": [ "native" ],
  },
)
