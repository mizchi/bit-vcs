import {
  "mizchi/bit" @git,
  "mizchi/bit/lib" @lib,
  "mizchi/bit/native" @gitnative,
  "mizchi/bit/osfs" @osfs,
  "mizchi/bit/x/hub" @hub,
  "mizchi/bit/x/hub/native" @hub_native,
  "mizchi/bit/x/agent" @agent,
  "mizchi/bit/x/fs" @bitfs,
  "mizchi/bit/protocol" @protocol,
  "mizchi/bit/pack" @pack,
  "moonbitlang/async" @async,
  "moonbitlang/async/process" @process,
}

warnings = "-29"

options(
  targets: {
    "cancellation_wbtest.mbt": [ "native" ],
    "runner.mbt": [ "native" ],
    "runner_wbtest.mbt": [ "native" ],
    "server.mbt": [ "native" ],
  },
)
