import {
  "mizchi/bit" @git,
  "mizchi/bit/lib" @lib,
  "mizchi/bit/x/hub" @hub,
}

import {
  "mizchi/bit/lib" @lib,
} for "test"

warnings = "-29"

options(
  targets: { "agent_test.mbt": [ "native" ] },
)
