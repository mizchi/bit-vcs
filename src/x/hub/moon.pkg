import {
  "mizchi/bit" @git,
  "mizchi/bit/lib" @lib,
  "moonbitlang/core/encoding/utf8" @utf8,
}

import {
  "mizchi/bit/lib" @lib,
} for "test"

warnings = "-29"

options(
  targets: { "sync_wbtest.mbt": [ "native" ] },
)
